import  espadas  from "/mini-rpg/src/js/armas/espadas.js"
import arcos from "/mini-rpg/src/js/armas/arcos.js"

const inventario = []

inventario.push(espadas[0])
inventario.push(arcos[0])
inventario.push(espadas[1])

console.log(inventario)

// ainda em construção, no agurado do modulo de export e import do roadmap que estou sguindo para finalizar a implementação.

export default inventario